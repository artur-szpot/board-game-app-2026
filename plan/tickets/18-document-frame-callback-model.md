# 18 Document frame callback model

## Description
Finalize and document the frame callback behavior for the frontend stack navigation system.

## Acceptance criteria
- `plan/output/frame_action_spec.md` is reviewed and confirmed.
- `OPEN_FRAME` and `CLOSE_FRAME` payload shapes are clearly defined.
- The callback flow for `BACK`, `CONFIRM`, and `close` behavior is validated.

## Notes
This ticket ensures frontend navigation behavior is stable before implementation.
